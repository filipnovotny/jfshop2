#!/bin/sh

GUNICORN=<%VIRTUALENVDIR%>
ROOT=<%CURRENTDIR%>
PID=/var/run/gunicorn.pid

APP=jfshop2.wsgi:application

if [ -f $PID ]; then rm $PID; fi

cd $ROOT
exec $GUNICORN -c $ROOT/gunicorn.conf.py --pid=$PID $APP